spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/users
    username: vpn
    password: vpn
    driverClassName: org.postgresql.Driver

server:
  port: 8787

security:
  basic:
    enabled: false
